from .base import * 

ALLOWED_HOST = []

INSTALLED_APPS += []

DATABASES = {
    "default": {
        "ENGINE": 'django.db.backends.postgresql_psycopg2',
        'NAME': '',
        'HOST': '',
        'PASSWORD': '',
        'USER': ''
    }
}
